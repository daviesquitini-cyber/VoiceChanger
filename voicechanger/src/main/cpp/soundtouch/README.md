# SoundTouch（第三方库）

- 来源：https://codeberg.org/soundtouch/soundtouch
- 版本：**2.4.1**（仅保留 Android 编译所需的 `include/` 与 `SoundTouch/` 核心源码，未做任何修改）
- 作者：Olli Parviainen
- 许可证：LGPL v2.1，见同目录 [COPYING.TXT](COPYING.TXT)

升级方式：从上游仓库对应 tag 覆盖 `include/` 与 `SoundTouch/` 两个目录即可，
JNI 绑定见上层的 `soundtouch-jni.cpp`。
